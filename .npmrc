# antd 6 declares React 18 peers while this project runs React 19.
# legacy-peer-deps lets the install/lockfile resolve consistently everywhere
# (local + Docker), matching how the lockfile was generated.
legacy-peer-deps=true
